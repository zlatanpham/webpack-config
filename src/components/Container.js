import React from "react";
import PropTypes from "prop-types";
import styles from "./style.scss";

const Container = props => {
  return (
    <div className={styles.container}>My Container this is the container</div>
  );
};

Container.propTypes = {};

export default Container;
